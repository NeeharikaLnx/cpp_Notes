User_Input:

cout << insertion operator
cin >> extraction operator


std :: string name;
int age;

std::cout << "what is your name ?"
std :: cin  >> name ;

std:: cout << "what is your age ?";
std:: cint >> age ;

std:: cout <<"Hello" << name << '\n';
std :: cout << "You are " << age << "years old";


std::cout << "What is your fullname ?:";
std:: getline(std::cin >> std:: ws , name);
return 0;

# std :: getline(std:: cin , name); helps to write the name with spaces

std::getline(std::cin >> std::ws , name); //ws is white spaces


--------------------------------------------------------------------------
Math functions :
#include <cmath>

double x=3;
double y=4;
double z;

z= std :: max(x,y);
z= std:: min(x,y);
std :: cout << z;
z=pow(2,3)
std:: cout << z;

z= sqrt(9);
z=abs(-40)

return 0;

Hypotenus of a triangle calculation :
c=sqrt(a2 +b2)

int main (){
double a ;
double b ;
double c;

std :: cout << "Enter Side A:";
std :: cin >> a;

std::cout << "Enter Side B:";
std:: cin >> b;
a=pow(a,2);
b=pow(b,2)
c=sqrt(a+b);

std::cout << "side C:" <<c;
return 0;

if statements:

#include<iostream>
int main(){
int age;
std:: cout << Enter your age" ;
std :: cin >> age;
if (age >=18){

  std:: cout << "Welcone to the site" ;
  
}
else if (age <0){
std:: cout<< "Yoou are not born yet";
}

else if (age >=100){
std :: cout << "You are too old to enter this site";
}

else{
  std:: cout << " You are not old enough ";
}



